import { EmailNotificationsPipe } from './email-notifications.pipe';

describe('EmailNotificationsPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailNotificationsPipe();
    expect(pipe).toBeTruthy();
  });
});
